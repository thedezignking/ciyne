# Optional — enables the "Find signature fields with AI" feature.
# Without it, the app works fully; the AI detection button shows an
# "not enabled on this deployment" message and signing stays manual.
#
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=
